#!/usr/bin/python3
"""Just a weird function to check something"""


def is_same_class(obj, a_class):
    """A function to check if an instance or not

    Args: The object to be checked
    The class or the target we can say
    """
    return (type(obj) is a_class)
